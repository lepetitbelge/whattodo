function createComment() {
  const newComment = "<%= j render partial: "comment", locals: { comment: @comment} %>"
  const commentNode = new DOMParser().parseFromString(newComment, "text/html").querySelector('li')
  document.querySelector('.show .todomodal-comments').appendChild(commentNode);

  document.querySelector('.show #comment_content').value = ""
};

createComment();

